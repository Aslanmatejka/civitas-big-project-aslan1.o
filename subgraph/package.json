{
  "name": "civitas-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for CIVITAS — DIDRegistry, Governance, Reputation",
  "scripts": {
    "codegen": "graph codegen",
    "build":   "graph build",
    "deploy":  "graph deploy --node https://api.studio.thegraph.com/deploy/ civitas",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 civitas",
    "create:local": "graph create --node http://localhost:8020/ civitas"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.65.0"
  }
}
